name: CI - VidaPlena Saúde Integrada

on:
  push:
    branches:
      - main
      - feat/**
  pull_request:
    branches:
      - main

jobs:
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Verificação básica
        run: |
          echo "Pipeline rodando com sucesso"
          echo "Projeto Front-end estático"

      - name: Lint HTML (HTMLHint)
        uses: cycjimmy/semantic-release-action@v4

      - name: Finalização
        run: echo "CI finalizado 🚀"
